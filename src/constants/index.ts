export enum UserProfileType {
  ADMIN = "AD",
  BUSINESS_OWNER = "BO",
  CLIENT = "CL",
  MANAGER = "MA",
}

export const enum BusinessEntityStatus {
  Active = "active",
  Pending = "pending",
  Suspended = "suspended",
}

export enum ModelStatus {
  Active = "active",
  InActive = "inactive",
}
